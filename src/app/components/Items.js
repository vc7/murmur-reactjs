import React from "react";
import { NoItem } from "./NoItem";
// import { Item } from "./Item";

export const Items = (props) => {
    return (
        <div>
            <NoItem/>
        </div>
    );
};